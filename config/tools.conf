# Tool paths and versions (optional overrides)
# Format: TOOL_NAME=path_or_command

subfinder=subfinder
amass=amass
httpx=httpx
nuclei=nuclei
nmap=nmap
massdns=massdns
subfinder_min_version=2.0.0
nuclei_min_version=3.0.0
